image: python:3.9-alpine

pages:
  stage: deploy
  script:
    - pip install -r docs/requirements.txt
    - mkdocs build
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
